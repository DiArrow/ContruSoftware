# Desarrollo / testing / CI — dependencias que NO se instalan en la imagen de producción.
# Instalar localmente con: pip install -r requirements.txt -r requirements-dev.txt
ruff>=0.4.0
sqlfluff>=3.0.0
yamllint>=1.35.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0,<0.28.0
