# Example environment variables for the backend service.
# Copy this file to `.env` and fill in real values.

POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_APP_USER=backend
POSTGRES_APP_PASSWORD=change_me
POSTGRES_DB=makerbox
